import React from 'react';
import ReactDOM from 'react-dom/client';
import EnshitificationPortal from './enshitification-portal.tsx';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EnshitificationPortal />
  </React.StrictMode>,
);
